import { Badge } from '@/components/ui/badge';
export function StubBadge() {
  return (
    <Badge variant="outline" className="text-amber-600 border-amber-400 bg-amber-50 text-xs font-normal">
      🧪 Demo stub — awaiting CurricuLLM API key
    </Badge>
  );
}
